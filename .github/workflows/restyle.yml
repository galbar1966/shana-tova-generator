name: Legacy site restyle disabled
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy restyle retired. Current UI is maintained directly in index.html."
